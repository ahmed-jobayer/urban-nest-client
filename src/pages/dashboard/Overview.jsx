
const Overview = () => {
    // const {user} = useAuth()

    return (
        <div className="flex items-center justify-center min-h-screen">
            <h3>{'user.email'}</h3>
        </div>
    );
};

export default Overview;